<div class="modal" id="modal-sign-up" tabindex="-1" role="dialog" data-show="false" style="display: none;">
  <div class="modal-body">
    <%= form_for @user, url: registrations_path(format: :json) do |f| %>
      <p class="field">
        <%= f.label :email, 'Tu Email' %>
        <%= f.text_field :email, placeholder: 'Email' %>
      </p>

      <p class="field">
        <%= f.label :password, 'Contrase;a' %>
        <%= f.password_field :password, autocomplete: 'off', placeholder: 'Contraseña' %>
      </p>

      <p><%= f.submit 'Crear Cuenta', class: 'btn btn-golden btn-block btn-large' %></p>
    <% end %>
  </div>
  <div class="modal-footer">
    <%= render partial: 'shared/auth_footer' %>
    <p class="register">¿Ya eres usuario? <a href="#" class="btn-sign-in">Iniciar Sesi&oacute;n</a></p>
  </div>
</div>
